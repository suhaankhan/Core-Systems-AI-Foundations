#include <iostream>

int main () {
    int binary;
    std::cout << "Enter Binary : ";
    std::cin >> binary;

    int value = 1;
    int decimal = 0;

    while (binary > 0) {
        int rem = binary % 10;
        value = value * rem;

        decimal = value

        
    }
}